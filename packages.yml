packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]

  # Uncomment to pull private Git packages (requires env.yml secrets + EAI):
  # - git: "https://{{env_var('DBT_ENV_SECRET_GIT_TOKEN')}}@github.com/your-org/your-repo.git"
  #   revision: main
